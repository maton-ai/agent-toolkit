"""Shared constants for Maton Agent Toolkit.

VERSION is the single source of truth for the package version: pyproject.toml
reads it via setuptools' dynamic version (``[tool.setuptools.dynamic]``), so
bump it here only.
"""

VERSION = "0.1.1"
MCP_SERVER_URL = "https://mcp.maton.ai"
TOOLKIT_HEADER = "maton-agent-toolkit-python"
MCP_HEADER = "maton-mcp-python"
